- # 📡 Echo Sanctuary Map Interface (回声避难所地图终端)

  > **"在大寂静之后，信息就是生命。"**

  本项目是一个沉浸式的交互地图应用，专为展示 **[Project Echo]** 的废土世界观而设计。用户将通过主角 **Jinx** 的视角，探索这片被电磁风暴和寡头封锁的废土世界。

  本项目基于 **React** + **Vite** 构建，利用 SVG 技术实现高性能的地图渲染与交互，并针对 Cloudflare Pages 进行了部署优化。

  ## 📖 项目背景 (Lore & Context)

  在被称为“大寂静”(The Great Silence) 的太阳风暴灾难后，旧文明崩溃，世界倒退回模拟信号时代。

  - **世界观**: 充满锈蚀与辐射的废土，由于持续的电磁干扰，长距离通讯断绝。
  - **核心冲突**: 掌握特权网络的“信息寡头” vs 试图重建自由通讯的幸存者。
  - **当前任务**: 玩家操作的是由游牧机械师 **Jinx** 组装的 "Echo Link" 终端，用于定位 **回声避难所 (Echo Sanctuary)**、标记危险的 **干扰者 (Interrupters)** 巢穴以及寻找旧世界科技的 **锈区 (Rust Zones)**。

  ## ✨ 功能特性

  - **废土朋克 UI**: 深度定制的 Dark Mode 界面，模仿老式 CRT 终端和示波器风格。
  - **交互式地图**: 支持丝滑的**平移 (Pan)** 和 **缩放 (Zoom)** (基于 `react-zoom-pan-pinch`)。
  - **动态标记 (Markers)**: 自动解析 Azgaar 地图导出的 SVG 数据，点击标记可弹出详细的情报窗口。
  - **响应式设计**: 完美适配桌面端与移动端操作。
  - **轻量级**: 纯前端架构，加载速度极快。

  ## 🛠️ 技术栈

  - **Core**: React 18, Vite
  - **Styling**: Tailwind CSS (用于快速构建废土风格布局)
  - **Map Engine**:
    - `react-inlinesvg` (SVG 注入与 DOM 操作)
    - `react-zoom-pan-pinch` (手势与视图控制)
  - **Icons**: Lucide React

  ## 🚀 快速开始 (Getting Started)

  ### 1. 环境准备

  确保您的电脑已安装 Node.js (推荐 v16+)。

  ### 2. 安装依赖

  在项目根目录下运行终端命令：

  ```
  npm install
  ```

  ### 3. 放置地图文件

将您从 Azgaar Fantasy Map Generator 导出的 SVG 文件重命名为 `map.svg`，并放入 `public/` 文件夹中。 *(注意：必须是 .svg 格式，且建议在导出前开启 Markers 和 Layers)*

> 也可以直接使用 CDN 源（默认已指向 `https://pub-c98d5902eedf42f6a9765dfad981fd88.r2.dev/map/nomeiland_jinx.svg`）。如需自定义，请在根目录创建 `.env` 并设置：
> ```
> VITE_MAP_SRC=https://your.cdn.com/your-map.svg
> ```

  ### 4. 启动开发服务器

  ```
  npm run dev
  ```

  打开浏览器访问 `http://localhost:5173` 即可预览效果。

  ## ⚙️ 自定义与配置

  ### 修改地图数据

  如果需要更新世界地图：

  1. 在 Azgaar 中编辑好 Labels 和 Markers。
  2. 导出新的 `.svg` 文件。
  3. 覆盖 `public/map.svg` 即可，无需修改代码。

  ### 修改交互逻辑

  核心逻辑位于 `src/App.jsx`：

  - **点击事件**: `handleMapClick` 函数负责解析 SVG DOM 结构。
  - **弹窗数据**: 可以在 `setSelectedMarker` 中接入后端 API 或读取本地 JSON 数据以展示更丰富的剧情文本。

  ## ☁️ 部署指南 (Deploy to Cloudflare Pages)

  本项目已针对 **Cloudflare Pages** 进行优化，这是托管静态 React 应用最简单且免费的方式。

  ### 方法 A: 通过 GitHub 自动部署 (推荐)

  这是最标准的方法，每次您 `git push` 更新代码，网站会自动重新构建。

  1. 在 GitHub 上创建一个新仓库 (Repository)。
  2. 将本项目代码推送到该仓库。
  3. 登录 [Cloudflare Dashboard](https://dash.cloudflare.com/)。
  4. 点击左侧菜单的 **Workers & Pages** -> **Create Application** -> **Pages** -> **Connect to Git**。
  5. 选择您刚才创建的 GitHub 仓库。
  6. **配置构建设置 (Build settings)**:
     - **Framework preset**: 选择 `Vite`
     - **Build command**: `npm run build`
     - **Build output directory**: `dist`
  7. 点击 **Save and Deploy**。

  ### 方法 B: 直接上传 (最快)

  如果您不想配置 Git，可以直接上传构建好的文件。

  1. 在本地终端运行构建命令:

     ```
     npm run build
     ```

     这会在项目根目录生成一个 `dist` 文件夹。

  2. 登录 [Cloudflare Dashboard](https://dash.cloudflare.com/)。

  3. 点击 **Workers & Pages** -> **Create Application** -> **Pages** -> **Upload assets**。

  4. 创建一个项目名（例如 `jinx-wasteland`）。

  5. 将本地的 `dist` 文件夹直接拖拽到上传区域。

  6. 点击部署，完成！

  ## 📂 项目结构

  ```
  .
  ├── public/
  │   └── map.svg          # 您的 Azgaar 地图源文件
  ├── src/
  │   ├── assets/          # 静态资源
  │   ├── App.jsx          # 主应用逻辑 (地图加载、交互、HUD)
  │   ├── index.css        # 全局样式 (Tailwind + 自定义地图样式)
  │   └── main.jsx         # 入口文件
  ├── index.html           # HTML 模板
  ├── package.json         # 依赖配置
  ├── tailwind.config.js   # Tailwind 配置
  └── vite.config.js       # Vite 构建配置
  ```

  *Project Echo © 2025. Built for the survivors.*